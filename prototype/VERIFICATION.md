# WMS 原型验证记录

验证日期：2026-07-03

## 自动化检查

- `node --test tests/state.test.mjs tests/integrity.test.mjs`：2 个测试文件通过，0 失败。
- `node --check app.js`、`state.js`、`data.js`：语法检查通过。
- 静态完整性：语义导航、主内容、实时通知、对话框、7 个核心视图和本地资源引用通过。

## 浏览器验证

入口：`http://127.0.0.1:4173/`

- 订单分配：选择 2 个订单后成功分配，页面显示 3 个已分配订单（含预置订单）。
- 波次释放：波次状态更新为“已释放”，3 个候选订单生成 6 个新拣货任务。
- 短拣恢复：选择“库位缺货 / 跨区重分配”后，状态更新为“观察中”。
- 重量异常：10.8kg 会即时显示错误并禁用发运；恢复 8.4kg 后解除阻断。
- 发运确认：包裹更新为“已发运”，运单回传反馈可见；状态层验证重复确认被阻止。
- 数字孪生：6 个仓库区域、5 个动态实体和任务路径可见；短拣处置后阻断告警由 1 变为 0。
- 入库管理：ASN 收货 48 件后更新为“待上架”，生成 2 个上架任务。
- 库存中心：冻结 3 件后可用库存从 128 更新为 125，并生成含原因、人员和时间的审计记录。
- 分析报表：收货、出库、库存准确率和任务效率指标从共享状态计算；吞吐趋势与数字孪生定位入口可用。
- 全模块导航：19 个业务视图可访问，覆盖 PRD FR-001～FR-028。
- 补货管理：任务释放成功，相同 SKU/目标库位需求自动合并。
- 盘点中心：盲盘不展示账面数量，差异 -9 件超过阈值后进入审批且未直接改库存。
- 退货中心：错误序列号被拦截；正确序列号可完成良品入库处置。
- 规则中心：相同范围和优先级的规则冲突被阻止；有效范围成功发布 v3。
- 集成平台：失败消息使用原幂等号重试成功，状态转为“成功”。
- 打印中心：受控重打要求原因，并记录操作者与次数。
- 权限与审计：未授权仓库访问返回 403 反馈；高风险审批与不可变日志可查看。
- PDA 作业：2 个离线操作恢复后安全同步，待同步归零且无重复事务。

## 交互与样式专项回归

- 数字孪生图层已改为真实内容切换：利用率显示容量热力，任务层显示 2 条路径，设备层显示 5 个实体，告警层显示异常定位且其他元素按层隐藏。
- 订单数据扩充为 23 条，主数据、库存、补货和规则列表均提供 3 页左右的演示数据；分页显示真实范围并切换不同记录。
- 订单状态/风险筛选、搜索、本页全选、报表时间范围均可操作。
- 驾驶舱、任务中心、主数据 Tab 使用独立状态，切换后更新标题、列表或数据结构。
- 顶部搜索、通知、仓库切换与账户入口均打开对应内容抽屉。
- 390px 移动端无页面横向溢出；普通表格首列不再被错误当作复选框定位，Tab 与分页支持横向滚动。
- 最终浏览器检查：非控制表格单元格空白数 0，控制台 error/warn 0。
- 驾驶舱首张 KPI 卡片已消除 `.brand` 类名冲突：恢复块级布局，高度 132px、标签宽度 232px，与其他卡片一致。
- 全局搜索已建立订单、库存、任务、入库、波次、异常和接口消息索引；支持中英文 ID/标题模糊匹配与结果跳转。
- 搜索“温湿度”返回库存与异常结果，点击库存结果成功进入库存中心并关闭搜索抽屉。
- ES 模块增加统一版本标识，避免原型迭代后浏览器混用新旧模块缓存导致空白页面。
- 安全治理中心拆分为审批中心、用户管理、角色管理、权限管理和审计日志五个可切换子模块。
- 用户存在 3 个未完成任务时停用被阻止；指定交接人后成功停用并生成 `user.deactivated` 审计事件。
- 仓库经理角色发布后从 v3 创建 v4，原版本保留，生成 `role.published` 审计事件。
- 权限模拟器对华南二号仓返回“数据范围”拒绝原因并写入拒绝审计。
- 审计日志展示 12 条分页数据，包含事件分类、前后值、结果与追踪 ID；浏览器控制台 0 错误。
- 安全治理按钮覆盖审计：10/10 个动作均有专属处理器，不再落入通用提示。
- “新建角色”可填写编码、名称、数据范围和初始权限，提交后创建 `R-QC-LEAD / v1 / 草稿` 并写入 `role.created` 审计事件。
- “新增用户”可填写账号、姓名、工号和初始角色，提交后创建 `wang.min / 待激活` 并写入 `user.created` 审计事件；重复账号/工号被阻止。
- 用户详情展示组织、仓库、岗位、认证来源、有效角色和登录时间线；角色权限说明展示职责、模块、操作、数据范围及限制条件。
- 安全态势报告展示账号状态、高风险权限、拒绝事件和安全建议，并支持脱敏导出反馈。
- 角色管理展示 13 个标准角色，覆盖管理、业务、现场作业、平台、安全和合规职责。
- 每个角色均可打开“权限说明”，查看职责、模块、关键操作、数据范围、限制、版本和发布状态。
- 图层与详情：人员设备图层可切换，区域/告警详情可打开。
- 键盘：详情抽屉支持 Escape 关闭并返回原焦点。
- 响应式：390×844 下显示移动导航，数字孪生画布宽 347px，文档宽 390px，无页面横向溢出。
- 桌面：1440×900 下数字孪生主画布宽 829px，侧栏、画布和态势面板完整呈现。
- 控制台：0 条 error/warn。

## 演示边界

原型使用虚构内存数据，不连接真实 OMS、TMS、WCS、打印机、电子秤或身份认证服务；刷新页面会重置演示状态。
